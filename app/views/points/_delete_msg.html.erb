<!-- Delete Point Dialog -->
<div class="modal fade" id="delete_dialog" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content">
      
      <!-- Header -->
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">
          <span aria-hidden="true">&times;</span>
        </button>
          <h4 class="modal-title" id="modal-label">確認!</h4>
      </div>
      
      <!-- Body -->
      <div class="modal-body">
        <% if current_user.id != @point.created_by %>
          <p>このポイントは、他のユーザが登録したデータです。</p>
          <p>十分確認の上削除してください。</p>
        <% end %>
        このポイントを削除してもよろしいですか
      </div>
      
      <!-- Footer-->
      <div class="modal-footer">
        
        <%= link_to 'Yes', {:controller => 'points', :action => 'destroy', :collection => @collection.id, :id => @point.id}, method: :delete, class: 'btn btn-danger' %>
        <button type="button" class="btn btn-primary" data-dismiss="modal">No</button>
      </div>
    </div>
  </div>
</div>